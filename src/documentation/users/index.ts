import { Tag } from 'swagger-jsdoc';
import path from './path';

const tag: Tag = {
  name: 'User Collection',
  description: 'This collection is going to /',
};

const user = {
  tag,
  path,
};

export default user;
